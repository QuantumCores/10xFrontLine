import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.tenxfrontline.app',
  appName: 'Front Line',
  webDir: 'dist/front-line/browser'
};

export default config;
